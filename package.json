{
  "name": "perdon",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "deploy": "npm run build && npx gh-pages -d dist",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.1.7"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
